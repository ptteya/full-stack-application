### 1. Start the Server
  ```bash
  cd server
  npm install
  npm start
  ```
 The server runs on http://localhost:5000.

### 2. Start the Client
```bash
cd client
npm install
npm start
```
Client runs on http://localhost:3000.
